import { LocaleSensitiveDirective } from './locale-sensitive.directive';

describe('LocaleSensitiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LocaleSensitiveDirective();
    expect(directive).toBeTruthy();
  });
});
